<?php

namespace {{ namespace }};

use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<{{ resource | ucFirst }}>
 */
class {{ resource | ucFirst }}Factory extends Factory
{
    protected $model = {{ resource | ucFirst }}::class;

    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        return [
            {% for cl in columns %}
            {% if cl.faker %}
            '{{ cl.name }}' => {{ cl.faker | fakerEval | raw }},
            {% endif %}
            {% endfor %}
        ];
    }
}
